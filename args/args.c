#include <stdio.h>
#include <string.h>

#define EXIT_SUCCESS 0;

enum expression { 
   NEW = 'n',
   DELETE = 'd',
   MOVE = 'm'
};

struct {
   char type;
   char* arg1;
   char* arg2;
}; ARG

struct *ARG getargs(char *argv[], int index);
int runargs(char *argv[], int index);


int main(int argc, char *argv[])
{

   return EXIT_SUCCESS
}

*ARG getargs(char *argv[], int index){
   for (int )
}
